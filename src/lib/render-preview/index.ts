/**
 * Render Preview — Public API
 *
 * Phase 2 / Sprint B：给生成的 HTML 截图，让还原度度量有真实的像素输入。
 * 详细背景见 ./render-html.ts 的文件头注释。
 */

export { DEFAULT_VIEWPORT, renderHtmlPage, renderHtmlScreenshot } from './render-html';
export type { RenderHtmlOptions, RenderedHtml } from './render-html';
