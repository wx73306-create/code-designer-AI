# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/

# production
build/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local
.env.production

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# deploy artifacts
deploy/deploy.zip

# deploy build artifacts
deploy/dist/
deploy/*.zip

# local debug artifacts (agent-browser screenshots, one-off probe scripts, backups)
.review/
_ui_shots/
_home_shot.png
_gen.js
_probe.js
_shot.js
pipeline_proof.mjs
repro_timeout.mjs
*.bak
*.bak.*

# 真机验收产物（本地，不入版本库）
.verify/
.verify-sprint3/
.build.log
.verify-run*.log

# 四道门禁日志（本地，不入版本库）
.gate-*.log

# 布局真值验收产物（本地，不入版本库）
.verify-layout/
.verify-*.log

# 还原度验收产物（本地，不入版本库）
.verify-reconstruction/

# 数据包归档产物（P1-10 / S-03）：runs/<jobId>/website-package/
# 内含真实站点的截图与设计 Token，属运行时产物，不入版本库
runs/

# B.2 样本批次工作目录：整个目录不入库。
# 原因（安全）：里面的 admin.txt / hdr.txt / sess.json 含有抓取到的 **admin_session cookie**，
# 属于凭据，绝不能被提交。日志还含模型原文，体积大且持续增长。
# 需要复现时按 docs/ 里的说明重建（POST /api/admin/login 即可重新拿到会话）。
.b2verify/
